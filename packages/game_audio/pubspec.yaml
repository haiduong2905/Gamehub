name: game_audio
description: Am thanh va khung man van dau dung chung cho moi game trong Game Hub.
version: 0.1.0
publish_to: none

environment:
  sdk: ^3.6.0
  flutter: ">=3.27.0"

dependencies:
  flutter:
    sdk: flutter
  audioplayers: ^6.8.1

  # Khung man van dau dung chung can doc PlayerClock cua core.
  # Core la pure Dart, khong phu thuoc gi, nen khong the sinh vong tron.
  platform_core:
    path: ../platform_core

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^5.0.0

flutter:
  assets:
    - assets/music/ambient.mp3
