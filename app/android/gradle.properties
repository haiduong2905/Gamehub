org.gradle.jvmargs=-Xmx8G -XX:MaxMetaspaceSize=4G -XX:ReservedCodeCacheSize=512m -XX:+HeapDumpOnOutOfMemoryError
android.useAndroidX=true
# This newDsl flag was added by the Flutter template
android.newDsl=false
# This builtInKotlin flag was added by the Flutter template
android.builtInKotlin=false

# Tat bien dich tang dan cua Kotlin.
#
# Kotlin 2.4 + Gradle 9.3 tren Windows lam do build voi loi
# "Could not close incremental caches ... class-fq-name-to-source.tab".
# Loi xay ra ngay ca khi build sach va khong co tien trinh nao chay song song.
# Du an nay it code Kotlin (chi cac plugin), nen mat mat toc do khong dang ke.
kotlin.incremental=false
